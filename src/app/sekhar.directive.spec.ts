import { SekharDirective } from './sekhar.directive';

describe('SekharDirective', () => {
  it('should create an instance', () => {
    const directive = new SekharDirective();
    expect(directive).toBeTruthy();
  });
});
